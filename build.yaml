targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            app_database: lib/core/local_database/app_database.dart
          schema_dir: drift_schemas/
          test_dir: test/drift/
